[filter "lfs"]
        clean = git-lfs clean -- %f
        smudge = git-lfs smudge -- %f
        process = git-lfs filter-process
        required = true
[user]
        name = Nuno do Carmo
        email = <email>
        signingkey = <key>
[commit]
        gpgsign = true
